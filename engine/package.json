{
  "name": "@bicamindlabs/fugunano-engine",
  "version": "0.0.0",
  "private": true,
  "description": "FuguNano typed engine for a repo-native multi-agent coding loop: 9+ LLMs, independent Codex review, bounded Self-Harness, no coordinator training.",
  "license": "Apache-2.0",
  "author": "BicaMind Labs",
  "type": "module",
  "engines": {
    "node": ">=18.18.0"
  },
  "bin": {
    "fugue": "./dist/cli/main.js"
  },
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint . && prettier --check .",
    "format": "prettier --write .",
    "check": "npm run typecheck && npm run lint && npm run test"
  },
  "dependencies": {
    "clipanion": "^4.0.0-rc.4"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@typescript-eslint/eslint-plugin": "^8.18.0",
    "@typescript-eslint/parser": "^8.18.0",
    "eslint": "^9.17.0",
    "fast-check": "^3.23.0",
    "prettier": "3.8.4",
    "tsup": "^8.3.5",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.18.0",
    "vitest": "^2.1.8"
  }
}
